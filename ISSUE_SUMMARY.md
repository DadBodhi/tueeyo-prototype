Event creation issues have been fixed:
- Form now sends correct field names (style/level/venue instead of style_id/level_id/venue_id)
- Added proper error handling to prevent form clearing on failure
- API endpoint already handles name-based creation correctly
